# -----------------------------------
# Dependencies
# -----------------------------------
node_modules/
package-lock.json # Optional: Keep this if you want locked versions, ignore if you strictly use yarn.lock
yarn.lock         # Optional: Keep if using Yarn, ignore if strictly using NPM
pnpm-lock.yaml    # Optional: Keep if using pnpm

# -----------------------------------
# Environment Variables & Security (CRITICAL)
# -----------------------------------
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem

# Note: It is best practice to commit a template file like .env.example 
# containing keys but NO values.

# -----------------------------------
# Build & Dist (Frontend/Backend)
# -----------------------------------
dist/
build/
out/
public/ # Only if 'public' is a generated build folder (common in some frameworks). If it holds source assets, keep it.
.next/
.nuxt/
.output/

# -----------------------------------
# Logs and Debugging
# -----------------------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# -----------------------------------
# Testing & Coverage
# -----------------------------------
coverage/
.nyc_output/

# -----------------------------------
# IDE & Editor Configuration
# -----------------------------------
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# -----------------------------------
# Operating System Files
# -----------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db